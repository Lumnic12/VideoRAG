# ══════════════════════════════════════════════════════════════════════════════
# Semantic Video Synthesizer — Python Dependencies
# ══════════════════════════════════════════════════════════════════════════════

# ── Core API Framework ──
fastapi>=0.115.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
aiofiles>=23.2.1
httpx>=0.27.0
pydantic-settings>=2.3.4

# ── Task Queue ──
celery>=5.3.6
redis>=5.0.4

# ── Video / Image Processing ──
opencv-python-headless>=4.9.0
scikit-image>=0.22.0
Pillow>=10.4.0
numpy>=2.0

# ── AI / ML ──
openai>=1.35.0
faiss-cpu>=1.8.0

# ── Logging ──
structlog>=24.2.0

# ── Testing ──
pytest>=8.2.2
pytest-asyncio>=0.23.8

# NOTE: These are installed separately in Dockerfile.backend because they are
# optional or platform-specific:
#   faster-whisper  — works on Linux, installed after core deps
#   winocr          — Windows-only (WinRT), skipped on Linux with || true
